[Unit]
Description=Chatapp Frontend Web Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/chatapp-infra/frontend
ExecStart=bash -c "npm install && npm run build && vite --port 80 && vite preview"

Restart=on-failure

[Install]
WantedBy=multi-user.target
